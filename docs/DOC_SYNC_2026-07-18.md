# Repo documentation sync — verified baseline (2026-07-18)

This is a **documentation-only** synchronization pass. Every factual claim in the
repo's own docs was verified against the **live database** (Supabase project
`lczmjpnbuhnsislcvzar`, read-only catalog introspection) or the **actual source
file**, on 2026-07-18. The live database was the only tiebreaker: where two docs
disagreed, or a doc disagreed with the code, the live catalog / source won. No
number was copied from another doc without re-checking it live.

**No code and no migration were touched.** All 87 changed files are Markdown
(`git status` shows zero non-`.md` changes). No DDL/DML was run against the
database — only read-only `SELECT` / catalog queries.

## How this was done
- Ground truth was first established live (counts, columns, constraints, config,
  invoice snapshot, CSRF behaviour, super-admin routes, billing-lockout interlock).
- Each in-scope Markdown file was then verified-and-fixed, then **independently
  re-reviewed by a second adversarial pass** that re-ran the riskiest queries.
  The reviewers caught and fixed 3 high-severity false statements the first pass
  had introduced or missed (see below).
- Living reference/skill/security docs were corrected thoroughly. Historical dated
  reports were preserved as records and given a dated point-in-time banner; only
  now-false current-state claims and dead references were corrected inline (the
  original audit numbers were never overwritten — the current live number was
  added alongside, dated).
- Every touched doc carries a sync banner and inline `(verified live 2026-07-18)`
  / `(UNVERIFIED: …)` markers so a reader can see what was checked.

## Landmines — each re-verified against live before writing
| Claim | Verified live 2026-07-18 |
|---|---|
| Zero-policy RLS tables | **22** (was documented as 18 / 21). Docs now state it dated + embed the recount query, since it moves as tables land. |
| `students.balance_due` | **Does not exist**; balances are computed live. Only `balance_alert_threshold`, `notify_on_balance` exist. |
| Late fees | **Dead** — the five `late_fee_*` `platform_config` keys are **deleted** (present 2026-07-15, absent now). Invoice columns (`late_fee_rate`, `late_fee_amount`, `days_overdue`) and orphaned `platformConfig_late_fee_*` i18n strings remain. |
| `summer.pay_window_days` / `summer.first_charge_release` | **1** / **HELD** |
| Billing lockout | **Built but inert** — suppressed behind the `PAYMOB_RECURRING_INTEGRATION_ID` placeholder. Documented as built-and-inactive, not live. |
| Migrations | Do **not** auto-apply to production on merge (preview branches only). Last prod-ledger migration: `20260717130000_billing_config_flip`. |
| Super-admin (4 routes) | Now derives the phone from the Supabase **auth identity** (`phoneFromCenterhqAuthEmail`), not tenant-writable `public.users.phone` (which has no `email` column). **Safe only while all logins are `@centerhq.local`** — documented as the load-bearing condition. |
| Invoice tax snapshot | Per-invoice `vat_rate` / `vat_amount` / `processing_fee`. **INV-007-2026-07** → vat_amount **125.26** (recompute 1020×0.14/1.14 = 125.26 ✓). |
| Terminology | Product **TutoringHQ** / **tutoringhq.app**; internal repo (CenterHQ), Vercel project (`center-hq`), `@centerhq.local` emails kept unchanged by design. |

## Live catalog counts used to reconcile the schema docs (counted live 2026-07-18)
142 base tables (all RLS-enabled) · 2 views · 141 functions in `public` (52 SECURITY
DEFINER) · 42 user triggers · 220 RLS policies · 512 indexes · **22 zero-policy RLS
tables**. Prior schema docs variously said 139/137 tables, 106/111/137 functions,
41/42 triggers, 221 policies, 300/489 indexes — all reconciled to live (historical
snapshots preserved and dated).

## Central fixes I made to the 6 items the review pass flagged for human decision
1. **`README-I18N.md` + `QUICK-START.md` + `scripts/download-fonts.md`** — font stack corrected: the product font is **IBM Plex Sans Arabic** (Arabic + Latin, ADR 031, `next/font/google`), not Cairo; Cairo-Arabic is a unicode-range fallback. JetBrains Mono woff2 **is copied** to `public/fonts/` but is **not wired** (`--font-mono` resolves to Plex; zero `jetbrains` refs in `src/`). Verified against `src/app/[locale]/layout.tsx` + `globals.css`.
2. **`docs/WA_TEMPLATES.md`** — the card-order checklist said "all six" but listed five; live has **7** `chq_card_order_*` seed rows (all PENDING): the five status rows + the `status_update` fallback + the deprecated `refunded` row. Corrected the count and clarified.
3. **`.claude/skills/client-onboarding-automation/SKILL.md`** — reconciled `chq_pin_delivery` wording with the product-reference skill: the Meta template **exists** (AUTHENTICATION, IN_REVIEW) but PIN delivery is **not wired live** (awaits Vodafone SIM + SMS fallback). Template registration vs delivery wiring are separate.
4. **`docs/audit/PAYMOB_LAUNCH_READINESS.md`** — `FEATURES.PAYMOB_ENABLED` is **env-driven** (`process.env.PAYMOB_ENABLED === 'true'`, `src/lib/features.ts:21`), unset in the repo so off by default — not a hardcoded `false`. The inert conclusion still holds via the placeholder recurring integration id.
5. **`docs/PRICING_SPEC.md`** — added a cross-reference clarifying that referral **withdrawals** carry their own separate cash-out fee, distinct from the invoice processing fee (referral payouts are correctly not charged the processing fee).
6. **`CLAUDE.md`** — removed one dead reference: `paygBilling.ts` (PAYG was removed; the file no longer exists). All other billing modules listed there were confirmed present. This was the only stale item found in `CLAUDE.md`, which otherwise already matches live (CSRF fail-closed, migrations-manual, VAT 14%, processing fee 20, super-admin phone-based).

## High-severity false statements the adversarial review caught and fixed
- **`docs/DB_PROXY_SECURITY.md` + `docs/SECURITY_MAINTENANCE.md`** — both said "none of the zero-policy tables are proxy tenant tables." Live catalog shows `attendance_overrides` (an *indirect* proxy join table in `TABLE_SCOPE`) IS zero-policy — corrected to state it is deny-all as a safe backstop.
- **`.claude/skills/tutoringhq-product-reference/SKILL.md`** — claimed five WhatsApp templates (`chq_nudge_*`, `chq_fee_reminder`) don't exist and must be created; live shows all five **already exist** as UTILITY/PENDING (need approval, not creation). Only `chq_pin_setup_link` + `chq_enrollment_otp` are genuinely absent.

---
## Docs changed and substantive corrections

Grouped by topic. Every factual claim was checked against the live Postgres catalog (project `lczmjpnbuhnsislcvzar`) or the actual source file on 2026-07-18; historical dated reports keep their record and gain a dated point-in-time banner. Each doc carries an inline sync banner + `(verified live 2026-07-18)` / `(UNVERIFIED: …)` markers.


### Schema & drift  

- **`docs/SCHEMA_BASELINE.md`**
  - Added a top sync banner (2026-07-18) flagging that the schema-baseline/drift system is current but the Phase-0 verification counts are a preserved snapshot, not present state.
  - Re-titled 'Phase 0 verification (recorded)' to mark it explicitly as a point-in-time snapshot and added a pointer to the current-live box.
  - Added an explicit resolution of the internal 106-vs-137 function inconsistency: 137 = total functions in public at Phase 0; 106 = app-owned subset the snapshot tracks; gap = 31 pg_trgm extension functions (106+31=137), which the snapshot excludes.
  - Added a boxed 'Current live counts (counted live 2026-07-18)' note: 142 base tables (all RLS-enabled), 141 functions in public (52 SECURITY DEFINER), 42 user triggers, 220 RLS policies, 512 indexes, 2 views; most recent prod-ledger migration 20260717130000_billing_config_flip; 22 zero-policy RLS tables with pointer to the recount query.
  - Qualified the 'How we got here' audit sentence as a Phase-0 point-in-time audit.
- **`docs/SCHEMA_GHOST_INVENTORY.md`**
  - Added a top point-in-time banner marking the 'at capture' counts (139 tables / 106 app functions +31 pg_trgm / 41 triggers / 221 policies / 489 indexes / 2 views) as Phase-0 history, with current live counts alongside (142 tables, 141 functions, 42 triggers, 220 policies, 512 indexes, 2 views; counted live 2026-07-18).
  - Added a 'Current RLS coverage — zero-policy tables' current-state addendum: 22 zero-policy RLS tables live 2026-07-18, the embedded recount SQL, and the full list of 22 table names; noted prior 18/21 counts are stale. This satisfies the cross-reference added in SCHEMA_BASELINE.
- **`docs/SCHEMA_HISTORY_TECHNICAL.md`**
  - Added a top point-in-time banner: the job's outcome still holds (users.pin_code gone, dropped by 20260701150506_drop_pin_code, verified live 2026-07-18), but the in-job catalog/ledger totals have moved; stated current live counts (142 tables / 141 functions / 42 triggers / 220 policies / 512 indexes / 2 views) and current last prod-ledger migration 20260717130000_billing_config_flip.
  - Tagged the final 'Snapshot: 6187 lines · 139 tables ... 111 functions' line as as-of-job (not current) with the current live figures inline.
- **`docs/SCHEMA_HISTORY_REPORT_FOR_EYAD.md`**
  - Added a top point-in-time banner marking it as an early-July-2026 completed-job record, confirming the still-true outcomes (pin_code gone, version mismatch fixed, rebuild-from-code works) as of 2026-07-18 and pointing to SCHEMA_HISTORY_TECHNICAL.md for current live counts.
- **`docs/tracker_disposition_v4.md`**
  - Added a top point-in-time banner (dispositions dated 2026-05-09 / 2026-05-21) and recorded that all referenced files were verified present live 2026-07-18: docs/DB_PROXY_SECURITY.md, docs/CARD_DESIGN.md, STOPPED.md, src/lib/dbProxyScope.ts, src/app/api/db/route.ts, scripts/security-audit.ts, scripts/i18n-backfill-stubs.ts.

### Security core  

- **`docs/CSRF_SETUP.md`**
  - Rewrote the 'Behavior' section: corrected the false claim that CSRF validation is 'skipped (development fallback)' when CSRF_SECRET is unset. Live code (src/lib/csrf.ts) fails CLOSED — validateCSRFRequest returns false → 403 in every environment, and getKey() throws in production when the secret is missing/malformed. Stated CSRF_SECRET is mandatory in all environments.
  - Added a dated correction note explaining the prior text was the opposite of actual behaviour.
  - Added synced-on-2026-07-18 banner.
- **`docs/DB_PROXY_SECURITY.md`**
  - Annotated TABLE_SCOPE with the verified live count: 21 tables total (17 direct, 2 indirect, 2 forbidden) per src/lib/dbProxyScope.ts.
  - Marked the indirect (student_group_members, attendance_overrides) and forbidden (demo_requests, whatsapp_incoming) sets as '2 tables' each, verified live.
  - Marked the follow-up item 'Add CREATE POLICY definitions for tenant tables that lack them' as DONE: all 14 listed proxy tenant tables now carry RLS policies (live counts: students 5, student_groups 8, attendance_scans/card_orders/reminder_settings/rooms/schedule_slots/subjects/wa_templates 4 each, paid_parents 3, permissions/student_group_members/subscriptions/whatsapp_messages 1 each). Cross-referenced the 22 zero-policy tables (2026-07-18) and the recount query now in SECURITY_MAINTENANCE.md.
  - Added synced-on-2026-07-18 banner.
- **`docs/SECURITY_MAINTENANCE.md`**
  - Corrected the offline-scanner PII section: removed balance_due from the cached-fields list (two occurrences). students.balance_due does not exist; src/lib/db.ts syncStudentsToLocal explicitly caches id/name/phone/groups/qr_code/student_number and notes balances are computed live via getStudentBalance (studentBalance.ts). Added a dated correction note.
  - Fixed the Phase 6 migration file references: replaced the non-existent paths '2026062600000{1,2,3}_phase6*' and '20260626000001'/'20260626000002' with the real on-disk filenames 20260626134248_phase6a_lockdown_definer_rpcs.sql, 20260626134256_phase6b_restrict_global_recompute.sql, 20260626134308_phase6f_tighten_anon_definer_funcs.sql.
  - Added a new 'RLS coverage — zero-policy tables' section with the embedded recount query and the live count (22 as of 2026-07-18), cross-referenced by DB_PROXY_SECURITY.md.
  - Added synced-on-2026-07-18 banner.
- **`docs/CRON_SECRET_VAULT.md`**
  - Added a synced-on-2026-07-18 banner confirming requireCronSecret.ts and cron-deembed-secret.sql are present, that the environment is still pre-go-live (so the 'known state' and 'go-live rotation' sections remain applicable), and clarifying that center-hq.vercel.app is the Vercel deployment URL (internal CenterHQ naming, kept) while tutoringhq.app is the public product domain.
  - Updated the '_Last updated_' line to record the 2026-07-18 verification pass.
- **`docs/AUDIT_center_role_gating.md`**
  - Added a point-in-time (2026-05-12) historical banner; recorded a 2026-07-18 review confirming requireCenterAuth still lives in src/lib/centerAuth.ts and that the three RECOMMENDED OWNER-ONLY routes (referrals/payout, card-order-cart/checkout, paymob/create-payment-key) are still ungated (no owner-role check present live).
  - Corrected the api/students/lifecycle row: the live students_lifecycle_status CHECK enum is enrolled/active/at_risk/inactive/churned; the audit's original 'enrolled → active → at_risk → churned' omitted 'inactive'.
- **`docs/ROTATION_LOG.md`**
  - Added a 2026-07-18 review banner recording that no rotation has been logged since the 2026-02-17 initial setup, consistent with SECURITY_MAINTENANCE.md's rotation-tracking table (all secrets still 'Last Rotated: 2026-02-17'; CSRF_SECRET/ANON_KEY next due 2026-08-17).

### Rules / ADR / tech reference  

- **`docs/CRITICAL_RULES.md`**
  - Removed dead reference to the non-existent snapshot directory `Full CenterHQ Files 18 May 2026/` (confirmed absent via find/ls at repo root and up to depth 3)
  - Added top verification banner (synced 2026-07-18)
  - Rule 139: annotated `isWeakPin` as defined at src/lib/weakPins.ts line 50 (verified live) after confirming all 5 PIN-setting route files exist and the function is at line 50
  - Rule 151: added that requireCenterAuth and requireTeacherAuth live in src/lib/centerAuth.ts (verified: line 99 and 345)
- **`docs/ADR.md`**
  - Added top point-in-time/verification banner
  - ADR 034: removed the stale '299 EGP/month' resubscribe figure and added a dated note that the standard plan is now 499 EGP/month gross per live platform_config.teacher_subscription_plan
  - ADR 035: retitled to note the two-tier decision is superseded; preserved the original June 2026 numbers as the record and added a current-live three-tier table (teacher_standard 499/trial14/limit20, teacher_pro 999/trial0/limit50/100 blast, teacher_scale 2499/limit100/+20 overage/100 blast) verified live 2026-07-18
- **`docs/CENTERHQ_TECHNICAL_REFERENCE_v21.md`**
  - Added top verification banner clarifying CenterHQ is the retained internal name vs TutoringHQ live brand
  - Rewrote the Migrations section to state migrations are MANUAL apply to production (Branching auto-applies to preview only, verified), require catalog confirmation before deploy, and noted last prod-ledger migration 20260717130000_billing_config_flip — replacing the thin 'apply via Supabase CLI in timestamp order' framing
  - Relabeled the API routes table header to note all listed routes were verified present under src/app/api on 2026-07-18
- **`docs/HELPERS_INVENTORY.md`**
  - Added top verification banner
  - Cairo section: removed non-existent `endOfCairoDay` (confirmed absent across all of src) and replaced with the actual verified exports — startOfCairoDay, cairoDateKey, startOfUtcInstantForCairoCalendarDay (day.ts); startOfCairoWeek, startOfCairoWeekKey, getCairoWeekDays (week.ts)
  - Locale routing: changed 'buildLocaleHref if present' to confirmed present at src/lib/locale/buildLocaleHref.ts with its signature (verified)
  - Auth/security: added concrete paths — requireCronSecret at src/lib/cron/requireCronSecret.ts, verifyHmac at src/lib/verifyHmac.ts (verified)

### Pricing / fees  

- **`docs/PRICING_SPEC.md`**
  - Bumped 'Last updated' 2026-07-07 → 2026-07-18 and added a top sync banner (product brand TutoringHQ / domain tutoringhq.app; CenterHQ internal names kept).
  - Plan price table: removed the stale 'Quarterly/mo' column (quarterly is no longer a selectable billing period — centers.billing_period CHECK IN ('monthly','annual'), verified live); tagged the Monthly column as live pricing_plans.all_in_price (solo 999·nano 1999·starter 4499·pro 7999·business 12999·enterprise 18499, top_centers NULL/custom) and annual_multiplier=10, all verified live 2026-07-18.
  - Added an explicit note that 'quarterly' now survives only as internal code terminology (PLANS[].quarterlyAllIn, getQuarterlyAllInMonthlyRateFromCenter) for the per-month all-in rate and legacy-row pricing.
  - Fixed the redesigned-invoice VAT worked example 125.16 → 125.14 (1019 × 0.14/1.14) and added the live INV-007-2026-07 snapshot (base 1000, total 1020, vat_rate 0.14, vat_amount 125.26, processing_fee 20) plus the per-invoice vat_rate/vat_amount/processing_fee snapshot-column fact, verified live.
  - Replaced the 'Late-fee / reactivation invoices (combined)' section: late fees documented as DEAD (zero late_fee_* platform_config keys live; invoices.late_fee_rate/late_fee_amount/days_overdue physically remain but dead) and reactivation split out as still-live (plain subscription + flat 20 EGP fee, verified in code; reactivationFee.ts deleted).
  - Rewrote the 'Pending / blocked (saved-card epic)' subsection: the MIT auto-charge is now BUILT and WIRED (Phase 1+2) but INERT behind the PAYMOB_RECURRING_INTEGRATION_ID placeholder — was wrongly described as 'unbuilt / no card token stored'.
  - …and 4 more corrections
- **`docs/SERVICE_FEE_REMOVAL_FINDINGS.md`**
  - Added a point-in-time banner: this build has SHIPPED (no SERVICE_RATE/STAMP_RATE in taxMath.ts; only VAT 14% + flat 20 EGP fee customer-visible, verified live 2026-07-18) and the authoritative end-state numbers are the Phase 3/5 figures (card flat 60 = 60/1.14; REFERRAL_NET_REVENUE_DIVISOR=1.14 VAT-only; referral payout (gross−20)×0.95, min 1000 EGP), NOT the intermediate Phase-1/2 proposals.
  - Demoted the original 'Status: BUILT … HOLD before any PR' line to a clearly-labelled historical status, since the branch has since merged.
  - Flagged that intermediate figures (Phase-2 card base 51.6, divisor 1.14×1.004) are superseded within the same doc and preserved only for the record.
- **`docs/CUSTOMER_INVOICES_AND_UNDERPAYMENT.md`**
  - Added a top sync banner recording the live-verified facts (owner-polymorphic invoices, amount_received NUMERIC NOT NULL DEFAULT 0, constraints invoices_owner_type_check + invoices_owner_xor_check, policies invoices_select_own_center + invoices_select_own_teacher, engine built-but-inert behind PAYMOB_RECURRING_INTEGRATION_ID placeholder).
  - Corrected three migration path references (20260625000000_teacher_invoices_parity.sql ×2, 20260624000000_invoice_amount_received.sql) from supabase/migrations/ to supabase/migrations_archive/ (folded into baseline) — the files no longer live in the active migrations dir.
  - Added (verified live 2026-07-18) tags to amount_received, owner_type polymorphism, and the invoices_select_own_teacher RLS policy.
- **`docs/SAVED_CARD_ENGINE.md`**
  - Retitled to 'Phase 1 + Phase 2', bumped date to 2026-07-18, and added a sync banner: the engine is BUILT, unit-tested, AND wired into the midnight cron but INERT (PAYMOB_RECURRING_INTEGRATION_ID placeholder verified 2026-07-18); the four tables are live with RLS on / zero user-facing policies (service-role only, verified live).
  - Changed the Phase-1 lead paragraph from 'NOT yet wired to any cron' to 'wired to the midnight cron (Phase 2) but INERT', resolving the in-doc contradiction with the Phase 2 section.
  - Corrected the schema migration reference to supabase/migrations_archive/20260624120000_saved_card_engine.sql (folded into baseline) and removed the stale 'committed but applied on review' note — the schema is already live.
  - Tagged the four tables and service-role/RLS status as verified live 2026-07-18.

### Billing & summer  

- **`docs/SUMMER_2026_FINDINGS.md`**
  - Added a dated sync banner noting the summer build has shipped (src/lib/summer/*, summerBillingCron, migration 20260628110833_summer_2026_promo_billing.sql, six summer.* keys, per-customer summer columns) with live config values.
  - Fixed the tax/fee claim: was 'VAT 14% + stamp 0.5% + service 6% cascading multiplication' — corrected to VAT 14% only, inclusive (base=inclusive/1.14); 6% service fee and 0.5% stamp duty removed; processing fee flat 20 EGP.
  - Updated section 7: 'No summer keys yet ... to add ... summer.pay_window_days (2)' — keys are now live; corrected pay_window_days to 1 and first_charge_release=HELD, with all six live values.
  - Corrected the usage->tier section: removed 'quarterly all-in prices per tier' framing, noted quarterly cadence retired (billing_period {monthly,annual}, subscription_billing_period {monthly,yearly}).
  - Removed stray trailing </content> and </invoke> template tags at end of file.
- **`docs/BILLING_RELIABILITY.md`**
  - Added dated sync banner confirming tamper trigger, both ledger tables, and reconciliation/nudge crons exist live.
  - Corrected migration path: 20260626000000_billing_reliability_hardening.sql is under supabase/migrations_archive/, not supabase/migrations/ (folded into baseline).
  - Added the live trigger name trg_chq_prevent_invoice_tampering (function chq_prevent_invoice_tampering) confirmed present on public.invoices.
- **`docs/BILLING_NUDGES.md`**
  - Added dated sync banner confirming src/lib/nudges/*, billing-nudges cron, NudgeBanner.tsx, and billing_nudges ledger table exist live; auto-charge still inert.
  - Corrected ledger migration path: 20260626120000_billing_nudges_ledger.sql is under supabase/migrations_archive/, not supabase/migrations/.
- **`docs/BILLING_PERIOD_MONTHLY_DEFAULT_FINDINGS.md`**
  - Added dated banner confirming the applied change is live: billing_period CHECK {monthly,annual} default monthly; subscription_billing_period CHECK {monthly,yearly} default monthly.
  - Fixed the migration filename: 20260705120000_billing_period_monthly_default.sql -> actual committed file is 20260705050120_billing_period_monthly_default.sql (verified present).
- **`docs/MONTHLY_ANNUAL_BILLING_FINDINGS.md`**
  - Added dated banner: this doc's 'deferred, no schema migration' decision is superseded; the schema change was later applied (migration 20260705050120) and is live — cross-referenced BILLING_PERIOD_MONTHLY_DEFAULT_FINDINGS.md.
  - Annotated the 'as-is' schema lines: billing_period is now default monthly / CHECK {monthly,annual}, subscription_billing_period now default monthly / CHECK {monthly,yearly}; struck the old quarterly defaults/checks while preserving them as historical.
  - Marked the 'CHECK & column defaults kept unchanged' decision as SUPERSEDED and recorded that the recommended PG17 follow-up was completed.
- **`docs/TEACHER_FREE_BASELINE_FINDINGS.md`**
  - Added dated banner: build shipped (migration 20260628135521_teacher_free_baseline_gate.sql); confirmed teacher_subscriptions.status CHECK {trialing,active,past_due,suspended,cancelled} and billing_interval CHECK {monthly,annual} live.
  - Removed stray trailing </content> template tag at end of file.

### Product / status / consent / i18n  

- **`docs/STUDENT_STATUS_SPEC.md`**
  - Added sync banner dated 2026-07-18 and inlined the exact live CHECK constraint students_lifecycle_status_check IN ('enrolled','active','at_risk','inactive','churned').
  - Added an explicit note that students.balance_due does NOT exist (balances computed live; only balance_alert_threshold + notify_on_balance are the balance columns).
  - Changed product brand 'CenterHQ student lifecycle filters' to 'TutoringHQ'.
  - Cross-referenced GUARDIAN_CONSENT.md and PARENT_SELF_ENROLL_CONSENT.md for consent columns.
- **`docs/GUARDIAN_CONSENT.md`**
  - Added sync banner confirming both columns present on public.students.
  - Tagged guardian_consent_confirmed_at/_by as verified present live 2026-07-18 in the data-model section.
  - Changed 'The center — not CenterHQ' to 'The center — not TutoringHQ' (product brand).
- **`docs/PARENT_SELF_ENROLL_CONSENT.md`**
  - Added sync banner confirming all three consent concepts are live public.students columns.
  - Tagged parent_self_enroll_consent_at as confirmed present live 2026-07-18 in the data-model section.
- **`docs/WA_TEMPLATES.md`**
  - Corrected stale support email: mailto:support@centerhq.com changed to mailto:support@ehgintelligence.com (live value SITE.supportEmail in src/config/site.ts).
  - Added sync banner; noted wa_meta_templates holds 45 rows (verified live).
  - chq_pin_delivery row: corrected category from 'auth' to 'AUTHENTICATION' and noted live status IN_REVIEW (doc had implied merely 'registered').
  - chq_card_order_refunded: noted the seed row currently exists as status PENDING (verified live), keeping the deprecated/do-not-register guidance.
  - Card-order-status checklist: noted all six chq_card_order_* rows confirmed status=PENDING live.
  - Nudge section: retitled to note the four chq_nudge_* rows already exist as UTILITY/PENDING (seeded, awaiting Meta approval) rather than 'new templates to submit'.
  - …and 2 more corrections
- **`docs/RTL.md`**
  - Changed title 'RTL and logical layout (CenterHQ)' to '(TutoringHQ)'.
  - Added sync banner confirming the --chq-slide-dir / --chq-slide-enter-tx motion tokens and admin-orders-slide-panel keyframe exist in src/app/globals.css (verified live).
- **`docs/E2E_TESTING.md`**
  - Corrected PLAYWRIGHT_BASE_URL example from https://centerhq.app to https://tutoringhq.app (retired product domain).
  - Added sync banner noting live product domain is tutoringhq.app.
- **`docs/LAUNCH_CHECKLIST.md`**
  - Changed title 'Launch checklist — CenterHQ' to '— TutoringHQ'.
  - Added sync banner noting environment is still pre-launch / test-data only and live domain tutoringhq.app.
  - Added note under the Paymob line that PAYMOB_RECURRING_INTEGRATION_ID is still a placeholder so the saved-card / billing-lockout path is built but inert.
- **`docs/DISASTER_RECOVERY.md`**
  - Added sync banner confirming all tables/columns used by the verification SQL are present live and that the verify-backup npm script exists.
  - Changed monthly-reminder calendar title 'Verify CenterHQ Database Backup' to 'Verify TutoringHQ Database Backup'.
  - Flagged Supabase backup frequency/retention as provider settings (UNVERIFIED from the app DB).
- **`docs/PAGE_INVENTORY.md`**
  - Title changed from 'TutoringHQ / CenterHQ — Full Page Inventory' to 'TutoringHQ — Full Page Inventory'.
  - At-a-glance Page routes 124 -> 128 (live find of src/app/**/page.tsx); localized URLs ~246 -> ~254 (127 localized x 2).
  - Settings section fully rewritten from 6 stale routes to the 12 live routes: added /settings/account, /settings/center, /settings/money, /settings/notifications, /settings/scanner, /settings/subjects, /settings/support; reframed /settings/general as the settings hub; documented /settings redirect TAB_REDIRECTS mapping only general/team/billing.
  - Removed /settings/reset-password entry (route no longer exists; PIN change now under /settings/account).
  - Super-Admin section header 31 -> 30; removed the /admin/sales-pipeline entry (route deleted); added /admin/staff (sales-staff directory: role sm/sr, base salary, YTD commission, territory).
  - Auth section header 11 -> 10; removed the /sentry-test entry (route deleted).
  - …and 3 more corrections

### Skills (.claude/skills)  

- **`.claude/skills/automated-billing-and-fees/SKILL.md`**
  - Added top sync banner (2026-07-18).
  - Rule 15: rewrote 'the five late_fee_* keys ... are legacy' to state the five keys were DELETED from platform_config (verified absent live 2026-07-18); only the unused invoices.late_fee_rate/late_fee_amount/days_overdue columns and orphaned platformConfig_late_fee_* i18n strings remain as dead legacy; added that the single-day lockout it references is itself BUILT BUT INERT behind the PAYMOB_RECURRING_INTEGRATION_ID placeholder.
  - Rule 14: tagged the per-invoice vat_rate/vat_amount/processing_fee snapshot as verified live 2026-07-18 with the INV-007 example (vat_rate 0.14, vat_amount 125.26, processing_fee 20) and constraint invoices_tax_snapshot_nonneg.
  - Rule 7: expanded plan anchors to the full ladder (Solo 999, Nano 1999, Starter 4499, Pro 7999, Business 12999, Enterprise 18499; Teacher 499/999/2499) and tagged verified live 2026-07-18.
  - Additional notes: flagged the single-day lock (billingLifecycle.ts/billingAccessGate.ts) as currently INERT behind the placeholder integration id (verified live 2026-07-18).
- **`.claude/skills/client-onboarding-automation/SKILL.md`**
  - Added top sync banner (2026-07-18) noting CenterHQ/@centerhq.local are retained internal/auth names while the product is TutoringHQ.
  - Invariant 3: replaced 'Quarterly card price is the signup baseline; the +15/−15% monthly/annual toggles are marketing approximations with .99 endings' with the current model — only monthly and annual offered, quarterly DEAD (centers.billing_period CHECK ('monthly','annual')), monthly baseline = pricing_plans.all_in_price, annual = monthly×10; noted quarterlyAllIn is a legacy variable name in SignupForm.tsx equal to the monthly price, and that no +15/-15% toggle or .99 ending exists.
- **`.claude/skills/ehg-algorithmic-asset-management/SKILL.md`**
  - Added a dated (2026-07-18) review banner clarifying this is an aspirational DRAFT framework with no shipped code or DB tables of its own, and that the only CenterHQ facts it borrows as analogies (processing_fee_enabled kill-switch flag, Cairo-time cron discipline, append-only audit tables) are current.
- **`.claude/skills/saas-multi-tenant-architecture/SKILL.md`**
  - Added top sync banner (2026-07-18) stating the zero-policy count is dated + query-backed because it moves.
  - Additional notes: added that the single-day lock (billingLifecycle.ts / billingAccessGate.ts) is currently BUILT BUT INERT behind the placeholder PAYMOB_RECURRING_INTEGRATION_ID — code paths wired but no center is actually locked out today (verified live 2026-07-18).
- **`.claude/skills/tutoringhq-product-reference/SKILL.md`**
  - Added top sync banner (2026-07-18) noting live product domain tutoringhq.app and retained internal CenterHQ names.
  - WhatsApp rules: corrected the category-fix list — chq_welcome and chq_onboarding_step1 are still MARKETING (need fix); chq_onboarding_step2 is ALREADY Utility (IN_REVIEW), removed from the fix list; chq_renewal_reminder is MARKETING/APPROVED (verified live) so contrary to the old note it STILL needs the Utility fix (old text wrongly said 'already Utility, do not refix').
  - Corrected 'chq_pin_delivery dropped off Meta' — it is now present as AUTHENTICATION/IN_REVIEW (verified live 2026-07-18); reframed the action as see-through-approval or remove the code path.
  - Added that chq_pin_setup_link and chq_enrollment_otp do not yet exist in wa_meta_templates (verified live) — 'must create' still accurate.
  - Launch step 4: tagged the summer first-charge gate as currently HELD (summer.first_charge_release=HELD, pay_window_days=1) verified live 2026-07-18.

### Subagents (.claude/agents)  

- **`.claude/agents/ciso-security.md`**
  - Priority 3 said 'CSRF_SECRET unset skips CSRF' — corrected to the opposite: an unset/malformed CSRF_SECRET FAILS CLOSED (validateCSRFRequest returns false, every mutation 403s in every environment, getKey throws in production), so a missing secret blocks all mutations rather than bypassing CSRF; cited src/lib/csrf.ts and tagged verified live 2026-07-18. Kept the Upstash 'falls open on rate limits' point (correct) and reframed the heading to 'Fail-open vs fail-closed fallbacks'.
  - Added a dated verification banner after the YAML frontmatter.
- **`.claude/agents/cfo-controller.md`**
  - Single-day lock bullet: annotated 'no late fees' with the fact that the five late_fee_* platform_config keys were deleted 2026-07-15..18 (feature dead; unused invoice columns and orphaned i18n keys remain), and added a NOTE that the billing-lockout/auto-suspend is BUILT BUT INERT because PAYMOB_RECURRING_INTEGRATION_ID is a placeholder — so 'unpaid -> lock at next Cairo midnight' does not currently execute. Tagged verified 2026-07-18.
  - Added a dated verification banner after the YAML frontmatter.
- **`.claude/agents/ceo-chief-of-staff.md`**
  - Added a dated verification banner after the YAML frontmatter noting tutoringhq.app as live product domain and EH_GROUP_MASTER_CONTEXT_v24.md present.
- **`.claude/agents/coo-operations.md`**
  - Added a dated verification banner after the YAML frontmatter.
- **`.claude/agents/cto-architect.md`**
  - Added a dated verification banner after the YAML frontmatter noting EH_GROUP_MASTER_CONTEXT_v24.md and PRICING_SPEC.md present.

### Root READMEs & onboarding  

- **`README.md`**
  - Replaced the entire create-next-app boilerplate README (Getting Started with yarn/pnpm/bun, `app/page.tsx`, Geist font, generic Vercel links) with an accurate TutoringHQ project entry point
  - Stated live product domain https://tutoringhq.app and explicitly kept internal CenterHQ names (repo CenterHQ, Vercel center-hq, @centerhq.local) as intentional, not bugs
  - Documented the real stack (Next 16.2, React 19.2, next-intl 4, etc.) and the actual npm scripts from package.json, including the check:* build gates (i18n:check + check:bidi + check:tolocale + check:balance-due + setup-fonts)
  - Noted middleware lives in src/proxy.ts (aliased), localePrefix 'always', and that migrations are manual-apply to production (verified 2026-07-18)
- **`README-I18N.md`**
  - Fixed the font description: not 'Google Fonts: Cairo for Arabic, Inter for English' — actual is self-hosted Cairo (Cairo-Arabic, Arabic glyphs only) + Georgia/system for Latin + JetBrains Mono monospace, copied from @fontsource by npm run setup-fonts (verified in globals.css + setup-fonts.mjs; no Inter font exists)
  - Corrected localePrefix from 'as-needed' to 'always' in the How It Works step 4 and reworked the URLs section (prefix is always shown; / redirects to /ar)
  - Changed middleware reference from src/middleware.ts to src/proxy.ts (aliased proxy.ts) in the structure block and How It Works
  - Removed dead references src/components/Navbar.tsx and src/lib/fonts.ts (both verified absent 2026-07-18); pointed font config to src/app/globals.css + scripts/setup-fonts.mjs
  - Updated the Adding-New-Locales font step and the Language Toggle section (no Navbar) accordingly
- **`QUICK-START.md`**
  - Same font correction as README-I18N: replaced 'Google Fonts: Cairo for Arabic, Inter for English' with the real self-hosted Cairo/Georgia/JetBrains Mono setup
  - Updated Key Files table: src/middleware.ts -> src/proxy.ts (aliased), added localePrefix 'always' note, removed dead src/components/Navbar.tsx and src/lib/fonts.ts and replaced with src/app/globals.css + scripts/setup-fonts.mjs
  - Tagged next-intl as v4
- **`BUILD_LOG.md`**
  - Added a HISTORICAL point-in-time banner (branch merged 2026-07-13); preserved all recorded numbers unchanged
  - Added dated live-state note: SW_VERSION is now v23 (recorded log ends at v18), production migration ledger runs through 20260717130000_billing_config_flip, and the two go-live switches remain pending so the saved-card engine is still INERT
- **`MERGE_CHECKLIST.md`**
  - Added a HISTORICAL banner (branch merged 2026-07-13)
  - Fixed internal contradiction: the intro said 'Nothing here has been applied to the live database; migrations are repo files only', but the Migrations section (and live catalog) show the five migrations ARE applied — reworded the intro to mark that framing superseded
  - Updated the go-live switches table with current status verified 2026-07-18: PAYMOB_RECURRING_INTEGRATION_ID still a placeholder (engine INERT) and summer.first_charge_release still HELD
- **`STOPPED.md`**
  - Added a HISTORICAL point-in-time banner (2026-05-09 pricing rollout)
  - Flagged the Post-deploy Enterprise query as STALE: public.centers has no plan_key and no monthly_price column today (only plan and all_in_price), so that query would error; confirmed the 18499 EGP/mo Enterprise baseline is still current via pricing_plans
  - Flagged the Starter B4 checkpoint: centers.monthly_price no longer exists; current Starter baseline 4499 lives in pricing_plans
- **`public/.well-known/README.md`**
  - Changed the live-served URL from https://centerhq.app/.well-known/assetlinks.json to https://tutoringhq.app/... (live product domain, verified 2026-07-18) and added a note that centerhq.app is retired while the Android package_name app.tutoringhq is unrelated to the retired domain
  - Added verification banner
- **`scripts/audit/README.md`**
  - Changed Production login URL from https://centerhq.app/en/login to https://tutoringhq.app/en/login (verified 2026-07-18; centerhq.app retired)
  - Added a verified note that centers has plan and all_in_price but NO plan_key and NO monthly_price columns, clarifying the seed's 'when columns exist' hedging
  - Added verification banner
  - Kept @centerhq.local auth-email format and SUPER_ADMIN_PHONES guidance unchanged (correct internal names)
- **`scripts/download-fonts.md`**
  - Added a verification banner confirming the six required filenames and their @fontsource sources match scripts/setup-fonts.mjs exactly (no factual corrections needed — doc was accurate)
- **`.cursor/plans/revenueguard_mvp_steps_7-14_85c33e1d.plan.md`**
  - Added an OBSOLETE/HISTORICAL banner (all todos already status: completed; superseded by shipped product) flagging the load-bearing stale points without rewriting the 1290-line plan: product is TutoringHQ (not RevenueGuard/CenterHQ as a product name); middleware is src/proxy.ts not src/middleware.ts; Next.js is 16.2 not 16.1.6; auth is phone+PIN over @centerhq.local, not the drafted phone-OTP + Google/Facebook OAuth flow

### Historical audits  

- **`docs/AUDIT_2026-07-02_REPORT_FOR_EYAD.md`**
  - Added dated point-in-time banner (snapshot 2 July 2026, reviewed 2026-07-18).
  - Top-10 item 10: annotated the 'pin_code column still exists in production' claim with a dated update that the drop has since reached production (users.pin_code no longer exists, verified live 2026-07-18) and the migration-drift half was addressed by the baseline-snapshot model.
- **`docs/AUDIT_2026-07-02_TECHNICAL.md`**
  - Added dated point-in-time banner.
  - Ground-truth surface line: annotated '137 public tables' with '142 base tables live 2026-07-18 (verified live)'.
  - H5 item 3: annotated 'users.pin_code still exists in production' with dated update that it no longer exists as of 2026-07-18 and the repo moved to a baseline-snapshot + archived-migrations model.
  - Verified-clean section: annotated '18 zero-policy tables' with the live count 22 (2026-07-18) and a 'state it as a live count, never permanent' caveat.
- **`docs/FIX_2026-07-02_C1_C2_H1_FINDINGS.md`**
  - Added dated point-in-time banner noting it is a historical fix-record for three specific findings; no demonstrably-false current-state claim required annotation.
- **`docs/FIX_2026-07-02_SAFE_CLEANUP_REPORT_FOR_EYAD.md`**
  - Added dated point-in-time banner; preserved as a historical before/after record.
- **`docs/FIX_2026-07-02_SAFE_CLEANUP_TECHNICAL.md`**
  - Added dated point-in-time banner.
  - Method/verification section: annotated the parked H5 delta 'live carries users.pin_code (drop migration unapplied)' with a dated update that the drop has since reached production (pin_code gone, verified live 2026-07-18) and the repo moved to a baseline-snapshot model, closing the parked delta.
- **`docs/CODEBASE_AUDIT_2026-07-05.md`**
  - Added dated point-in-time banner; recorded a 2026-07-18 spot-check that src/lib/admin-check.ts and /financial-intelligence still exist, so the SAFE-delete proposals are not yet executed and the findings stand.
- **`docs/ENTERPRISE_ARCHITECTURE_AUDIT_2026-07-07.md`**
  - Added dated point-in-time banner.
  - Section 1 key-correction: annotated 'CLAUDE.md still describes tax as cascading multiplication' with a dated update that CLAUDE.md now states 'Tax is 14% VAT only, inclusive' — that follow-up is done.
  - B-H1 (two contradictory VAT formulas): added a dated update that it is FIXED — taxMath.ts now uses P*0.14/1.14 via VAT_DIVISOR=1.14 (read directly this session), matching processingFee.vatInsideInclusive, with ground-truth INV-007 vat_amount 125.26 corroboration.
- **`docs/AUDIT_n_plus_1_hotpath_may13.md`**
  - Added dated point-in-time banner.
  - File 2 (admin/centers DELETE N+1 on parent_portal_tokens): added a dated update that it is FIXED — the delete was batched to a single .in('student_id',…) and the DELETE handler was later rewritten to deactivate rather than hard-delete, cross-referencing ENTERPRISE §3.4 and SAFE_CLEANUP_TECHNICAL §H3.
- **`docs/CLEANUP_BATCH_5_FINDINGS.md`**
  - Added dated point-in-time banner.
  - Part 3.1 zero-policy RLS section: annotated '21 tables' with a dated update that the live count is now 22 (2026-07-18) — billing_lockout_events joined the set with the built-but-inert billing-lockout ledger — plus a 'recount live, not permanent' caveat; relabeled the 21-table list 'as of this batch'.

### Historical design/cleanup  

- **`docs/DARK_MODE_REMOVAL_2026-07-05.md`**
  - Added a dated banner noting this doc's 'machinery KEPT for still-dark auth pages' state is superseded by the 07-06 leftovers cleanup and the auth-pages cream redesign; recorded live 2026-07-18 confirmation that LoginThemeEffect.tsx and contexts/ThemeContext.tsx are gone, next-themes is absent from package.json, and there are zero Tailwind dark: variants in src (only QR color:{dark,light} object props remain)
- **`docs/DARK_MODE_LEFTOVERS_CLEANUP_2026-07-06.md`**
  - Added a dated banner confirming the end state still holds live 2026-07-18: LoginThemeEffect.tsx and contexts/ThemeContext.tsx gone, next-themes absent, zero Tailwind dark: variants in src (residual dark-keyed strings are QR color:{dark,light} object keys in students/page.tsx, students/print/PrintClient.tsx, teacher/QrCodeBlock.tsx, generateInvoicePdf.ts)
- **`docs/TOKEN_ADOPTION_2026-07-05.md`**
  - Added a dated banner confirming the single-cream-theme premise still holds live 2026-07-18 (no next-themes, no .dark machinery, no Tailwind dark: variants) and marking the counts/batch-log/§5 follow-ups as point-in-time
- **`docs/AUTH_PAGES_CREAM_REDESIGN_2026-07-05.md`**
  - Added a top banner flagging Follow-up Item 2 as stale
  - Added a dated correction to Follow-up Item 2: the 6% service fee and 0.5% stamp duty have since been removed everywhere — taxMath.ts no longer defines SERVICE_RATE or a stamp constant, VAT 14% inclusive is the only tax (VAT_RATE=0.14, base=inclusive/1.14), platform_config.lesson_commission={vat_pct:0.14,teacher_pct:0,customer_pct:0,processing_flat:0}; preserved the original investigation text unedited
- **`docs/CENTER_PORTAL_REPAINT_CLASSIFICATION.md`**
  - Added a dated banner marking the route classifications and 5 reported bugs as point-in-time (2026-07-07, Phase-2 held for review) and re-confirming live the DB fact behind bug #3: centers_billing_period_check=IN('monthly','annual') and centers_subscription_billing_period_check=IN('monthly','yearly') (quarterly retired at DB layer)
- **`docs/BANNER_MATCH_FINDINGS.md`**
  - Added a dated banner: summer.promo.enabled is now true live 2026-07-18 (was OFF when written), so the ribbon+popup gate is ON; also confirmed first_charge_release=HELD, first_charge_floor=2026-08-30, pay_window_days=1 live
  - Corrected the §5 bullet 'It is OFF now → live page stays clean' to note it is ON as of 2026-07-18
  - Removed stray malformed trailing </content></invoke> tags at end of file
- **`docs/CARD_DESIGN.md`**
  - Corrected receipt-PDF pricing lines from 'subtotal → service → stamp → VAT → total' to 'subtotal → VAT → total' — service/stamp lines removed; only VAT 14% inclusive remains (verified live in taxMath.ts buildLegalInvoiceLines)
  - Corrected receipt filename pattern from centerhq-order-<LAST8>.pdf to tutoringhq-order-<LAST8>.pdf (last 8 hex of order id, uppercased — verified in orders/[orderId]/receipt/route.ts)
  - Added a top sync banner
- **`docs/DEAD_CODE_REMOVED_2026-07-05.md`**
  - Added a dated banner confirming key deletions still hold live 2026-07-18 (contexts/ThemeContext.tsx, components/ThemeToggle.tsx, onboarding components gone; /financial-intelligence still resolves as a redirect) and marking the 'unused dependencies' list as a point-in-time to-do
- **`docs/CODE_LOOSE_ENDS_FINDINGS.md`**
  - Added a top banner marking Item 3 as superseded
  - Added a dated UPDATE note to Item 3: the recommended follow-up was carried out — pin_set_at added, readers migrated, pin_code DROPPED (migration 20260701150506_drop_pin_code); live public.users has no pin_code and carries phone/role/pin_set_at
  - Removed stray malformed trailing </content></invoke> tags
- **`docs/PIN_SET_CLEANUP_FINDINGS.md`**
  - Added a dated banner: the end state is now live — pin_set_at exists and pin_code has been DROPPED (migration 20260701150506_drop_pin_code); resolves the §4 caveat that the drop was 'tracked but NOT applied to prod in this session'; noted live public.users now has 4 rows vs the historical '6 users'
  - Removed stray malformed trailing </content> tag
- **`docs/GROUP_PROPOSALS_MERGE_FINDINGS.md`**
  - Added a dated banner confirming Option 1 shipped and holds live: src/components/group-proposals/ exists with types.ts, OfferHistory.tsx, CounterOfferForm.tsx; cross-referenced the §3 token drift to CENTER_PORTAL_REPAINT_CLASSIFICATION.md §8
- **`docs/READINESS_FIXES_2026-07-03.md`**
  - Added a dated banner: WhatsApp-template submission tallies were introspected 2026-07-03 and are point-in-time (wa_meta_templates now 45 rows live 2026-07-18); confirmed the retired-domain safety net (code defaults to tutoringhq.app not centerhq.app) and that dormancy_trigger_day=30 / dormancy_data_retention_months=12 config keys still exist
- **`docs/POST_LAUNCH_LINT_DEBT.md`**
  - Added a dated banner marking the relaxed-rule ledger as point-in-time
  - Corrected the dead reference in §1: struck src/contexts/ThemeContext.tsx as a set-state-in-effect site — the file was deleted in the dark-mode removal (verified live: ls not found)
- **`docs/BUILD_COST_FINDINGS.md`**
  - Added a dated banner confirming the chosen settings landed and hold (.github/dependabot.yml exists live 2026-07-18) and marking the specific closed dependabot PR numbers as a point-in-time backlog

### Briefs / launch-readiness / business  

- **`docs/briefs/2026-07-15_skills_pack_and_rules.md`**
  - Added a SHIPPED/HISTORICAL banner: the three skills now live under .claude/skills/, the four working rules are in CLAUDE.md, and Job 2 produced the sweep + Job 4 draft — with dated live deltas (late fees now DELETED not merely unreachable; billing lockout BUILT but INERT on the placeholder recurring integration id; zero-policy RLS count 22 not 18, counted live 2026-07-18)
  - Flagged that the brief text is preserved verbatim and its embedded 'current state' lines (incl. skill invariant 15's 'late fees unreachable' framing) must not be read as live
- **`docs/briefs/2026-07-16_job4_money_batch.md`**
  - Added a SHIPPED/HISTORICAL banner confirming the batch executed (live balance card, invariants 16/17 in the billing skill) and noting two of its snapshot lines have since moved: summer.pay_window_days now 1, and the late_fee_* keys since deleted
- **`docs/findings/2026-07-15_sweep.md`**
  - Added a point-in-time banner (investigated 2026-07-16, re-synced 2026-07-18)
  - Annotated the live-catalog line 'five late_fee_* keys still live (tier1 day4/tier2 day9/grace3)' with a dated UPDATE: those five keys have since been DELETED, late fees are dead (verified live 2026-07-18) — preserving the original 2026-07-16 reading
  - Annotated 'summer.pay_window_days=2' with dated UPDATE to 1 (verified live 2026-07-18)
- **`docs/audit/PAYMOB_LAUNCH_READINESS.md`**
  - Added a point-in-time banner (2026-05-23) with a live launch-state note: Paymob still NOT live (FEATURES.PAYMOB_ENABLED false, PAYMOB_RECURRING_INTEGRATION_ID placeholder), so saved-card auto-charge and billing lockout remain inert
  - Replaced the retired live product domain centerhq.app with tutoringhq.app in all 4 actionable URLs (summary register line, canonical/deprecated handler lines, launch-day register step)
- **`docs/audit/SET_PIN_ONBOARDING_DESIGN.md`**
  - Added a point-in-time banner: design SHIPPED (src/app/[locale]/set-pin/page.tsx + SetPinClient.tsx exist, verified 2026-07-18)
  - Flagged that public.users.pin_code NO LONGER EXISTS (dropped by migration 20260701150506_drop_pin_code) so all references below to the pin_code NULL-gate and bcrypt mirror write are stale schema; live credential is the Supabase Auth password with users.pin_set_at as the marker
  - Replaced centerhq.app with tutoringhq.app in the two Set-PIN link URLs and the 'reopens centerhq.app' line; kept @centerhq.local auth emails
  - Noted customer-facing brand copy should read TutoringHQ (sample WhatsApp bodies still say CenterHQ)
- **`docs/audit/WHATSAPP_LAUNCH_READINESS.md`**
  - Added a point-in-time banner (2026-05-23) noting the Section-1 RED 'PIN never delivered' finding was since addressed by the set-PIN-on-first-login flow, and that users.pin_code was dropped (20260701150506) so the 'bcrypt-hashed into users.pin_code' mechanics are stale schema
  - Recorded wa_sending_enabled = true live (verified 2026-07-18)
  - Replaced centerhq.app with tutoringhq.app in the 4 launch-day URLs (both webhook registrations, sync-templates POST, health curl); kept @centerhq.local
- **`docs/CenterHQ_Business_Plan_v5.md`**
  - Added a top banner marking the pricing table and all revenue projections SUPERSEDED and cross-referencing docs/PRICING_SPEC.md
  - Added a SUPERSEDED note in Section 4 with live monthly all-in prices (Solo 999, Nano 1999, Starter 4499, Pro 7999, Business 12999, Enterprise 18499, Top Centers custom), annual = monthly x 10, VAT 14% inclusive (base = inclusive/1.14) + flat 20 EGP processing fee, and noted there are no per-tier setup fees of the kind tabled
- **`docs/EH_GROUP_MASTER_CONTEXT_v24.md`**
  - Added a banner marking this a STALE in-repo copy (v24) whose canonical (v46) lives outside the repo, and noting quarterly billing is now DEAD (centers.billing_period CHECK IN monthly/annual only, verified live 2026-07-18)
  - Annotated the '90 logical entities / ~89 migrations' line as STALE with the live count: 142 base tables (all RLS-enabled), 2 views, 141 functions, counted live 2026-07-18
- **`docs/EH_GROUP_STRATEGIC_ROADMAP.md`**
  - Added a banner marking it a forward-looking vision doc (not current operational state) with two live clarifications: TutoringHQ's live monetization is a SUBSCRIPTION model (tiered all-in monthly/annual + flat 20 EGP processing fee, VAT-inclusive), not the 'transaction-fee' model the body implies; and the Mauritius/Georgia structure + EHG Intelligence/treasury activation are PARKED/DRAFT pending Adsero + tax review (cross-ref the ehg-algorithmic-asset-management skill marked STATUS: DRAFT)
  - Noted the companion EH_GROUP_MASTER_CONTEXT_v24 is a stale copy (canonical v46 out of repo)

## Contradictions found and resolved (all resolved by the live DB/code)

- **`docs/SCHEMA_BASELINE.md`** — Internal 106 (snapshot coverage) vs 137 (production total) function counts — resolved by explaining the 31 pg_trgm-function gap rather than forcing one number.
- **`docs/SCHEMA_BASELINE.md`** — Phase-0 index counts differ across this doc (300 snapshot coverage) and GHOST_INVENTORY (489 live at capture); left both as their respective historical measurements and added the single current-live figure (512) dated.
- **`docs/SCHEMA_GHOST_INVENTORY.md`** — 489 indexes here vs 300 in SCHEMA_BASELINE Phase-0 section — different because one is live-at-capture and the other is snapshot coverage; left both as historical and added current-live 512.
- **`docs/SCHEMA_HISTORY_TECHNICAL.md`** — 106 functions (Phase 0) vs 111 functions (end of this job) in snapshot coverage — both left as their dated point-in-time snapshot values; current live (141) added.
- **`docs/CSRF_SETUP.md`** — Doc contradicted src/lib/csrf.ts and GROUND_TRUTH; resolved in favour of live code (fail-closed).
- **`docs/DB_PROXY_SECURITY.md`** — Follow-up claimed 14 tenant tables lacked policies; live pg_policies shows all 14 now have policies. Resolved via live DB — marked DONE.
- **`docs/SECURITY_MAINTENANCE.md`** — Doc listed balance_due as a cached scanner field; src/lib/db.ts states the opposite. Resolved via live code — removed balance_due.
- **`docs/SECURITY_MAINTENANCE.md`** — Doc's Phase 6 migration paths did not exist on disk; corrected to actual filenames found via find.
- **`docs/AUDIT_center_role_gating.md`** — lifecycle enum in doc omitted 'inactive' vs live constraint; resolved by adding the full live enum inline.
- **`docs/ADR.md`** — ADR 034 (299/mo) and ADR 035 (two-tier 299/699) contradicted the live platform_config three-tier 499/999/2499 pricing — resolved by preserving the historical decision and appending dated live values
- **`docs/HELPERS_INVENTORY.md`** — Doc listed endOfCairoDay as a Cairo helper but no such export exists in src — resolved by replacing with the actual helper names (CLAUDE.md also lists endOfCairoDay; that is a code/CLAUDE.md discrepancy left untouched per scope)
- **`docs/PRICING_SPEC.md`** — Within PRICING_SPEC, SUBSCRIPTION_GRACE_CALENDAR_DAYS stated as both 7 (grace section) and 1 (lifecycle section) — resolved to 1 against src/lib/billingSchedule.ts.
- **`docs/PRICING_SPEC.md`** — Saved-card described as 'unbuilt / not wired' in the lifecycle 'pending' block but 'wired, inert' in the bottom sections — resolved to built+wired+inert against live tables and code.
- **`docs/SERVICE_FEE_REMOVAL_FINDINGS.md`** — Doc's own phase progression carried conflicting intermediate numbers (card 51.6 vs 60; divisor 1.14×1.004 vs 1.14); resolved by stating the shipped end-state up top and marking earlier phases superseded, without altering the historical tables.
- **`docs/SAVED_CARD_ENGINE.md`** — Phase-1 intro said 'not wired to any cron' while the Phase-2 section said it is wired-but-inert; resolved to wired+inert against live tables and the placeholder integration id.
- **`docs/SUMMER_2026_FINDINGS.md`** — Doc's taxMath description (14%+0.5%+6% cascading) contradicted ground truth (14% inclusive only) — resolved in favor of live/ground truth.
- **`docs/MONTHLY_ANNUAL_BILLING_FINDINGS.md`** — Doc's claim that the schema was left unchanged contradicts live (schema was changed) — resolved by dated superseded-banner + inline annotations, preserving the original narrative.
- **`docs/WA_TEMPLATES.md`** — Doc's mailto (support@centerhq.com) contradicted the live code (support@ehgintelligence.com); resolved to the code value.
- **`docs/E2E_TESTING.md`** — centerhq.app presented as a live target deployment contradicts the retired-domain fact; resolved to tutoringhq.app.
- **`docs/PAGE_INVENTORY.md`** — Old Super-Admin header (31) did not match its 30 listed entries; resolved to live count 30.
- **`docs/PAGE_INVENTORY.md`** — Doc claimed /admin/sales-pipeline and /sentry-test exist; live catalog shows both removed — resolved by deletion.
- **`.claude/skills/automated-billing-and-fees/SKILL.md`** — Doc implied the five late_fee_* config keys still exist ('are legacy'); GROUND_TRUTH + rationale confirm they were deleted 2026-07-15..18. Resolved by rewriting rule 15 to 'DELETED', keeping the dead invoice columns/i18n strings noted.
- **`.claude/skills/client-onboarding-automation/SKILL.md`** — Invariant 3 conflicted with GROUND_TRUTH (quarterly removed, monthly/annual only, annual×10) and with SignupForm.tsx/plans.ts (quarterlyAllIn = the monthly price, no percentage toggle). Resolved in favor of live code + catalog.
- **`.claude/skills/tutoringhq-product-reference/SKILL.md`** — Doc said chq_renewal_reminder was 'already Utility'; live wa_meta_templates shows MARKETING. Doc listed chq_onboarding_step2 as needing a Utility fix; live shows it already UTILITY. Doc said chq_pin_delivery 'dropped off Meta'; live shows it present (AUTHENTICATION/IN_REVIEW). All three resolved to live state.
- **`.claude/agents/ciso-security.md`** — Doc's 'CSRF_SECRET unset skips CSRF' contradicted GROUND_TRUTH.md CSRF section (fails closed). Resolved in favor of live code/ground truth.
- **`.claude/agents/cfo-controller.md`** — Doc implied the single-day lock is active policy; GROUND_TRUTH shows the lockout is inert behind the Paymob placeholder. Resolved by adding the inert-status NOTE rather than deleting the policy description.
- **`README.md`** — Boilerplate implied a generic Next.js starter with no product identity; resolved by rewriting against CLAUDE.md + live package.json/routing
- **`README-I18N.md`** — Doc's 'as-needed' + 'default locale hides prefix' contradicted live routing.ts ('always'); resolved to 'always'
- **`MERGE_CHECKLIST.md`** — Intro 'nothing applied to live' vs. 'Migrations ALL APPLIED to live (2026-07-13)' within the same doc — resolved in favor of applied (live-verified)
- **`STOPPED.md`** — Doc's operational SQL assumed plan_key/monthly_price columns that no longer exist; resolved by dated stale-query notes (numbers preserved, live column names given alongside)
- **`docs/AUDIT_2026-07-02_REPORT_FOR_EYAD.md`** — 'pin_code still exists' contradicted by ground truth (dropped by 20260701150506) and by this group's CLEANUP_BATCH_5_FINDINGS which already records it dropped — resolved by dated inline note, live wins.
- **`docs/AUDIT_2026-07-02_TECHNICAL.md`** — Table count 137 vs live 142, and zero-policy 18 vs live 22 — reconciled with dated inline live numbers; live database wins.
- **`docs/FIX_2026-07-02_SAFE_CLEANUP_TECHNICAL.md`** — Parked 'pin_code unapplied' delta contradicted by ground truth (dropped) — resolved with dated inline note, live wins.
- **`docs/ENTERPRISE_ARCHITECTURE_AUDIT_2026-07-07.md`** — Stale-CLAUDE.md tax note vs current CLAUDE.md (now VAT-only), and B-H1 vs current taxMath.ts (÷1.14) — both resolved with dated inline notes; live code/CLAUDE.md win.
- **`docs/CLEANUP_BATCH_5_FINDINGS.md`** — Zero-policy 21 vs live 22 — reconciled with dated inline note naming billing_lockout_events as the added table; live database wins.
- **`docs/DARK_MODE_REMOVAL_2026-07-05.md`** — Directly contradicts DARK_MODE_LEFTOVERS_CLEANUP_2026-07-06.md (this keeps the machinery, that deletes it); resolved by dating the supersession in a banner rather than editing the narrative — live state matches the 07-06 end state
- **`docs/DARK_MODE_LEFTOVERS_CLEANUP_2026-07-06.md`** — Resolves the contradiction with DARK_MODE_REMOVAL_2026-07-05.md — this doc's deletion is the live state
- **`docs/AUTH_PAGES_CREAM_REDESIGN_2026-07-05.md`** — Item 2's 'SERVICE_RATE=0.06 is real' contradicts GROUND_TRUTH/live taxMath.ts; resolved by dated correction preserving the historical finding
- **`docs/BANNER_MATCH_FINDINGS.md`** — 'summer.promo.enabled OFF' contradicts live true (GROUND_TRUTH); resolved by dated correction
- **`docs/CARD_DESIGN.md`** — Doc's centerhq-order filename vs live tutoringhq-order; and service/stamp lines vs live VAT-only — both resolved by live code
- **`docs/CODE_LOOSE_ENDS_FINDINGS.md`** — Item 3 'recommend keeping pin_code' contradicts live drop and PIN_SET_CLEANUP_FINDINGS.md; resolved by dated forward-reference note
- **`docs/PIN_SET_CLEANUP_FINDINGS.md`** — §4 'drop not applied' vs live drop applied; resolved by dated banner, historical numbers preserved with current count added
- **`docs/briefs/2026-07-15_skills_pack_and_rules.md`** — Brief-embedded skill invariant 15 frames late fees as 'unreachable under billing lockout'; live state is stronger — the 5 late_fee_* config keys are deleted. Resolved by dated banner delta, leaving verbatim skill text intact.
- **`docs/briefs/2026-07-16_job4_money_batch.md`** — Brief snapshot 'summer.pay_window_days=2' era vs live=1: this brief predates the change; reconciled via banner rather than altering the brief body (it does not state pay_window itself)
- **`docs/findings/2026-07-15_sweep.md`** — Doc's 2026-07-16 catalog reading of late_fee_* keys and pay_window_days directly contradicts GROUND_TRUTH 2026-07-18; the live DB wins, applied as dated inline UPDATEs (historical-doc rule: keep the number found, add the current one dated)
- **`docs/audit/PAYMOB_LAUNCH_READINESS.md`** — Doc registered centerhq.app as the live webhook URL; GROUND_TRUTH: live product domain is tutoringhq.app. Live wins — corrected.
- **`docs/audit/SET_PIN_ONBOARDING_DESIGN.md`** — Design assumes users.pin_code is a live nullable column; live schema has dropped it. Live wins — corrected via banner note across the affected references.
- **`docs/audit/WHATSAPP_LAUNCH_READINESS.md`** — Doc's PIN model (mint+deliver PIN, store in users.pin_code) contradicts live (pin_code dropped; set-pin-on-first-login). Live wins — reconciled via banner; internal audit body left as historical record.
- **`docs/CenterHQ_Business_Plan_v5.md`** — Doc's Section 4 tiers (Starter 2000 / Pro 4500 / Business 6500 / Enterprise 9000, per-tier setup fees) contradict live pricing_plans. Live wins — marked superseded with live figures; projection tables preserved as historical.
- **`docs/EH_GROUP_MASTER_CONTEXT_v24.md`** — Table count 90 vs live 142; quarterly-baseline pricing vs live monthly/annual-only. Live wins — corrected the count inline and flagged quarterly-dead in the banner; per stale-copy instruction, did not recreate/rewrite the numbered narrative.
- **`docs/EH_GROUP_STRATEGIC_ROADMAP.md`** — 'transaction-fee monetization model' vs the live subscription model. Live wins — reconciled via banner note; strategic narrative left intact as vision.

## Claims I could NOT verify from the live DB or code (open questions for consolidation)

None were papered over — each is left in its doc marked UNVERIFIED / point-in-time. They fall into: historical build/audit measurements not re-run; provider-managed settings (Vercel env, Supabase backups, Meta approval state, Google Drive); runtime behaviors (route 403 gating, GoTrue, CI workflow logic) not exercised in a doc pass; and external market data.

- **`docs/SCHEMA_BASELINE.md`** — The Phase-0 fidelity measurements (6169-object empty diff, 2851 table grants, 289 routine grants, md5 fingerprints) are historical point-in-time results I did not re-run; preserved as history, not claimed as current.
- **`docs/SCHEMA_GHOST_INVENTORY.md`** — The specific ghost object name lists (47 tables, 41 functions, 15 triggers) are the Phase-0 detection output; I did not re-run the git-history cross-check that produced them — preserved verbatim as historical fact.
- **`docs/SCHEMA_HISTORY_TECHNICAL.md`** — In-job measurements (ledger 235/237 rows, users=6 rows, pin_code non-null=0, 1081 unit tests, md5 fingerprint 7378a91e..., PG16 rebuild equality) are point-in-time job results not re-verifiable now; preserved as the job's record.
- **`docs/SCHEMA_HISTORY_TECHNICAL.md`** — Function-body drift resolution for is_teacher_private_locked / process_due_subscriptions (comment-only) not re-checked against live prosrc this session.
- **`docs/SCHEMA_HISTORY_REPORT_FOR_EYAD.md`** — The plain-language proof claims (1081 tests pass, fingerprint match, 0 pending changes) mirror the technical doc's in-job results and were not re-run.
- **`docs/tracker_disposition_v4.md`** — The 316-findings count and per-finding dispositions are the audit tracker's own record (source of truth is audit_tracker.jsx, not in repo); not independently re-verifiable and preserved as-is.
- **`docs/tracker_disposition_v4.md`** — Whether the code-level behaviors described (e.g. forced scoping actually enforced) still hold was not re-audited — only file existence was checked.
- **`docs/DB_PROXY_SECURITY.md`** — The historical 'read/update/delete any centre's rows on the 20 allow-listed tables' (pre-2026-05-21 vuln narrative): left as-is; the table count at that past date cannot be reconstructed from the live catalog. Current count (21) added in the Tenant isolation section instead.
- **`docs/SECURITY_MAINTENANCE.md`** — Rotation-tracking table 'Last Rotated'/'Next Due' dates and the Drive/Backup secret inventory: operational bookkeeping, not verifiable from the DB catalog or source. Left unchanged; banner flags them as bookkeeping.
- **`docs/SECURITY_MAINTENANCE.md`** — Supabase linter advisory items (leaked-password OFF, pg_net/pg_trgm in public): described as accepted decisions; not re-run this session.
- **`docs/CRON_SECRET_VAULT.md`** — Whether the scheduled pg_cron jobs are currently returning 401 / the Vault cron_secret vs Vercel CRON_SECRET mismatch: not queried live this session (net._http_response / vault not inspected). Left as the doc's documented pre-go-live state with the pre-go-live banner.
- **`docs/AUDIT_center_role_gating.md`** — The full 60-route inventory and the per-category counts (18 GATED-OWNER, 30 ASSISTANT-OK, 7 NEEDS-REVIEW, etc.): not re-audited route-by-route this session. Preserved as the historical snapshot; banner flags them as as-of-2026-05-12.
- **`docs/ROTATION_LOG.md`** — Whether an unlogged rotation actually occurred: cannot be verified from the DB or source; the log itself is the only record and shows none since setup.
- **`docs/CRITICAL_RULES.md`** — Rule 144 cream design hex tokens (#ece8df/#fffdf8/#0e6b61/#9a6b1f) in globals.css — not re-verified this pass; cosmetic, not a DB fact
- **`docs/CRITICAL_RULES.md`** — Rule 152 GoTrue 500-on-null-token-fields behavior — runtime GoTrue behavior, not checkable from catalog
- **`docs/ADR.md`** — ADR 018 Google Shared Drive backup migration — external Google Drive/Workspace state, not checkable from repo/DB
- **`docs/ADR.md`** — ADR 035 group/feature caps (student notes, CSV export, lifetime history) — feature-flag/UI behavior, not catalog-verifiable
- **`docs/CENTERHQ_TECHNICAL_REFERENCE_v21.md`** — security-audit-skip PR label behavior in CI — GitHub Actions runtime config, not verified this pass
- **`docs/PRICING_SPEC.md`** — getImpliedMonthlyMrr being used by exactly /api/admin/billing, /api/admin/overview, /api/admin/finance — did not open each caller; function exists in pricing.ts
- **`docs/PRICING_SPEC.md`** — snapshot-mrr / subscription-autocharge cron schedule '0 22 * * *' in vercel.json — not opened (left as stated)
- **`docs/PRICING_SPEC.md`** — 'PAYG billing removed in 2026-07' historical claim — not independently datable, but no live contradiction
- **`docs/SERVICE_FEE_REMOVAL_FINDINGS.md`** — The historical test counts (1147/1147, 1152/1152, 1153/1153) and 'no PR until Eyad approves' notes — left intact as the point-in-time build record, not re-run
- **`docs/SERVICE_FEE_REMOVAL_FINDINGS.md`** — BLAST_SERVICE_FEE_RATE = 0.06 in src/lib/parentPack.ts being untouched — not re-opened this pass (out-of-scope flag in the doc)
- **`docs/CUSTOMER_INVOICES_AND_UNDERPAYMENT.md`** — teacher_id FK → teacher_profiles(user_id) ON DELETE CASCADE and the specific partial indexes — constraint/index existence spot-checked at column level only, FK target not re-introspected this pass
- **`docs/CUSTOMER_INVOICES_AND_UNDERPAYMENT.md`** — 'before/after fingerprint verified; ends NOTIFY pgrst' build-time claims — historical, not re-run
- **`docs/SAVED_CARD_ENGINE.md`** — card_charge_intents idempotency_key UNIQUE and saved_cards.card_last4 exactly-4-digit CHECK — not re-introspected (build detail; not contradicted)
- **`docs/SAVED_CARD_ENGINE.md`** — vercel.json registration of subscription-autocharge at '0 22 * * *' — not opened this pass
- **`docs/SAVED_CARD_ENGINE.md`** — Unit-test assertions (token stored never PAN, etc.) — historical build claims, not re-run
- **`docs/BILLING_RELIABILITY.md`** — webhook_inbox / webhook_outbox / dead_letter_queue idempotency machinery described in the 'already in place' audit — not independently re-verified this session (not flagged as suspect).
- **`docs/BILLING_PERIOD_MONTHLY_DEFAULT_FINDINGS.md`** — Whether the two quarterly writers (admin/centers approve, payg-billing cron) were actually edited to stop writing 'quarterly' — doc claims they were fixed; live CHECK now forbids quarterly, but I did not re-read those source files to confirm the code no longer emits it.
- **`docs/MONTHLY_ANNUAL_BILLING_FINDINGS.md`** — Reference 'all_in_price equals PLANS[].quarterlyAllIn' names a code field; did not verify the field is still named quarterlyAllIn in current source (historical narrative, left as-is).
- **`docs/TEACHER_FREE_BASELINE_FINDINGS.md`** — Existence/exact bodies of RLS predicates teacher_private_access, is_teacher_private_locked, apply_teacher_subscription_transition — not re-introspected this session (design-doc detail, not flagged as suspect).
- **`docs/GUARDIAN_CONSENT.md`** — Server-enforcement behavior of each /api route (403 GUARDIAN_CONSENT_REQUIRED etc.) was not re-executed; only the DB columns were verified. Route files exist per repo but runtime gating not exercised in a doc sync.
- **`docs/PARENT_SELF_ENROLL_CONSENT.md`** — Route-level gating (403 PARENT_CONSENT_REQUIRED) and rate-limit '10/hour' not re-executed; only DB columns verified.
- **`docs/WA_TEMPLATES.md`** — Future/proposed template names (chq_payment_reminder, chq_payment_due_today, chq_payment_overdue, chq_subscription_suspended, chq_payment_received) are not in wa_meta_templates yet — consistent with being 'future', so left as-is.
- **`docs/WA_TEMPLATES.md`** — Meta-side registration/approval state (as opposed to the DB status column) cannot be read from the database.
- **`docs/RTL.md`** — ESLint no-restricted-syntax deferral status is a policy/plan statement, not DB/code-verifiable; left as future-plan note.
- **`docs/E2E_TESTING.md`** — CI workflow claims (test.yml/ci.yml continue-on-error / non-blocking Playwright) were not opened this session; left as-is.
- **`docs/E2E_TESTING.md`** — Existence of individual spec files (admin-pages.spec.ts, card-order-full.spec.ts) not enumerated, only the project names.
- **`docs/LAUNCH_CHECKLIST.md`** — Presence/absence of individual Vercel env vars (WHATSAPP_APP_SECRET, BOSTA_WEBHOOK_SECRET, etc.) cannot be read from the database; left as the pre-launch task list.
- **`docs/DISASTER_RECOVERY.md`** — Supabase automated-backup schedule (daily 2:00 AM UTC) and retention (7d free / 30d pro) are Supabase-managed provider settings not exposed in the app database.
- **`docs/DISASTER_RECOVERY.md`** — Emergency contact placeholders ('Your phone number', 'Your co-founder') are intentionally generic.
- **`docs/PAGE_INVENTORY.md`** — Per-page feature bullets (KPIs, modal contents, LOC estimates) were not line-audited for every one of 128 pages — only route existence and the changed areas were verified; unchanged descriptive bullets are carried forward as-is.
- **`docs/PAGE_INVENTORY.md`** — Descriptions of the seven new settings sub-pages were derived from translation-key inspection, not full page read.
- **`.claude/skills/automated-billing-and-fees/SKILL.md`** — Paymob's 2.75% + 3 EGP transaction cost — already flagged in the doc as an unconfirmed assumption; cannot verify EHG's negotiated rate.
- **`.claude/skills/automated-billing-and-fees/SKILL.md`** — Eyad-decision attributions (2026-07-15/16) — internal decisions, not checkable against DB/code.
- **`.claude/skills/client-onboarding-automation/SKILL.md`** — Card style tiers 'only B (Dark) and C (Light) exposed, A reserved' — product/UI decision, not represented in the live DB.
- **`.claude/skills/client-onboarding-automation/SKILL.md`** — chq_pin_delivery 'is a stub / no live PIN blasts' — operational sending state, not verifiable from DB (template row exists as AUTHENTICATION/IN_REVIEW).
- **`.claude/skills/ehg-algorithmic-asset-management/SKILL.md`** — mrr_snapshots table referenced only as an analogy ('like mrr_snapshots') — not verified live; left as-is since it is illustrative, not a load-bearing claim.
- **`.claude/skills/tutoringhq-product-reference/SKILL.md`** — chq_referral_commission 'wrong language tag (English tag, Arabic body)' — wa_meta_templates has no language column, cannot verify from DB; tagged UNVERIFIED in the doc.
- **`.claude/skills/tutoringhq-product-reference/SKILL.md`** — Design-system claims (PR #146 light-theme-only, teal tokens, 8 page patterns) — code/UI decisions not checkable against the DB in this pass.
- **`.claude/skills/tutoringhq-product-reference/SKILL.md`** — Template body language/comma-char rules (Arabic EGY, U+060C) — not represented in the queried table columns.
- **`.claude/agents/cfo-controller.md`** — Fee-booking policy (20 EGP fee is revenue netting 17.54, Paymob % is cost of sales, Paymob 2.75%+3 EGP unconfirmed) — a stated principal/business decision (Eyad 2026-07-16), not a DB/code fact; left as-is unchanged.
- **`.claude/agents/cfo-controller.md`** — Referral commission base = all_in_price/1.14 — consistent with VAT model but no live referral rows to confirm computed values; left as-is.
- **`.claude/agents/coo-operations.md`** — 'Bosta shipping is a pass-through reimbursement above tax' — operational policy statement; not contradicted by ground truth, no live shipping rows to confirm; left as-is.
- **`PVC_Card_Printing_Vendors_Egypt.md`** — Vendor phone numbers, pricing, and company details are external market data not verifiable against the DB or codebase; group guidance says leave factual vendor data as-is
- **`docs/AUDIT_2026-07-02_REPORT_FOR_EYAD.md`** — The narrative money-loophole PASS claims (nine switching guardrails, referral farming, promo single-use, credit non-negative) were not re-verified against current code this session; preserved as historical findings.
- **`docs/AUDIT_2026-07-02_TECHNICAL.md`** — 319 API routes / 42 cron routes / 123 pages counts not re-counted this session (point-in-time, banner covers).
- **`docs/AUDIT_2026-07-02_TECHNICAL.md`** — Per-route CRITICAL/HIGH exploit findings (C1 IDOR, C2 anon read, H1 auth bypass) not re-verified against current route code; the companion FIX doc records them fixed.
- **`docs/FIX_2026-07-02_C1_C2_H1_FINDINGS.md`** — Whether the content_access_log policy rescope and the C1/H1 route fixes are live in current code was not re-queried this session; preserved as the doc's own fix record (banner-scoped).
- **`docs/FIX_2026-07-02_SAFE_CLEANUP_REPORT_FOR_EYAD.md`** — Whether the applied migrations (append-only commission_audit_log, money CHECK>=0, FK RESTRICT, parent-portal hardening) remain live in the current catalog was not re-queried this session.
- **`docs/FIX_2026-07-02_SAFE_CLEANUP_TECHNICAL.md`** — Section A/C/D/E migration outcomes (numeric(12,2) retypes, upgrade_log UNIQUE, token_hash column, hot-path indexes) were not re-introspected live this session.
- **`docs/CODEBASE_AUDIT_2026-07-05.md`** — jscpd/knip metrics (90 clones, 45 unused files, 405 unused exports) and the 124-page/322-API/177-lib counts were not re-run this session (tooling output, point-in-time).
- **`docs/ENTERPRISE_ARCHITECTURE_AUDIT_2026-07-07.md`** — C1 (annual-as-monthly signup double-bill), C2 (announcement_settlement free-extend), T-H1 (onboarding center factory), T-H2 (/api/db self-unsuspend), B-H2 (auto_suspend_at date truncation), B-H3 (one-shot renewal invoice), B-H4 (top_centers NULL-price on renewal) were not re-verified against current code this session — left as recorded historical findings, NOT asserted fixed.
- **`docs/ENTERPRISE_ARCHITECTURE_AUDIT_2026-07-07.md`** — '272 indexes' deployment-velocity count not re-counted (ground truth shows 512 live 2026-07-18; point-in-time, banner-scoped).
- **`docs/AUDIT_n_plus_1_hotpath_may13.md`** — Current admin/centers/route.ts DELETE handler was not read directly this session; the 'fixed' note rests on two corroborating group docs rather than a fresh source read.
- **`docs/CLEANUP_BATCH_5_FINDINGS.md`** — The hand-rolled-auth route inventory (12 center/org + 3 mixed) was not re-scanned this session; note that PR #176 has since hardened super-admin derivation on 4 of them (analytics/revenue, admin/overview, admin/pending-signups, admin/centers) per ground truth, but the doc's suspension-gate consolidation recommendation is a distinct still-open concern, so left as-is.
- **`docs/DARK_MODE_REMOVAL_2026-07-05.md`** — Whether the specific i18n keys common.switchToDarkTheme/switchToLightTheme were removed — not checked live; not load-bearing
- **`docs/DARK_MODE_LEFTOVERS_CLEANUP_2026-07-06.md`** — The historical counts (~840 dark: occurrences across 68 files; unit-test 1153/142) are point-in-time and left as-is
- **`docs/TOKEN_ADOPTION_2026-07-05.md`** — §5 claim that tokens.ts still carries dark-era values (surface[0]='#080f1a', text.primary='#f8fafc') and the ~3,450 Tailwind-utility count — not re-verified; flagged as point-in-time follow-ups in the banner
- **`docs/AUTH_PAGES_CREAM_REDESIGN_2026-07-05.md`** — Whether the signup.serviceFee i18n label string still contains '6%' today — not checked live (messages/*.json out of edit scope); the correction states the underlying fee is gone regardless
- **`docs/CENTER_PORTAL_REPAINT_CLASSIFICATION.md`** — Current code state of the 5 reported bugs (doubled ## on student detail, quarterly PeriodCard still rendered, /api/billing/upgrade accepting quarterly, price mismatch, duplicate Change PIN) — these are Phase-2 items not re-verified against current source this session
- **`docs/BANNER_MATCH_FINDINGS.md`** — Whether the SummerRibbon/SummerPopup components still render exactly as described (Fraunces font, gradient) — component internals not re-verified; only the config gate state was corrected
- **`docs/CARD_DESIGN.md`** — 06:00 Cairo equivalence for the 04:00 UTC cron — Egypt DST makes July UTC+3 (07:00), left unchanged as marginal
- **`docs/CARD_DESIGN.md`** — card_orders refund_status/status/payment_status enum values not exhaustively re-checked against live catalog
- **`docs/DEAD_CODE_REMOVED_2026-07-05.md`** — Whether the flagged unused deps (@anthropic-ai/sdk, jspdf, canvas-confetti, @fontsource/*) are still present in package.json — not re-checked; framed as a point-in-time to-do
- **`docs/CODE_LOOSE_ENDS_FINDINGS.md`** — Items 1,2,4,5,6 (referral_commissions shape, join route conflict, auth-gate migration, dependency overrides, linter) not re-verified against current source; left as point-in-time branch state
- **`docs/PIN_SET_CLEANUP_FINDINGS.md`** — The exact backfill outcome (which of the historical 6 users got pin_set_at) — historical, preserved; only the drop landing was confirmed
- **`docs/GROUP_PROPOSALS_MERGE_FINDINGS.md`** — Exact current line counts of the two source screens (731/786) — historical, not re-checked
- **`docs/READINESS_FIXES_2026-07-03.md`** — Per-template APPROVED/IN_REVIEW/PENDING status for the 13+16 templates — not re-checked per row; wa_meta_templates total is 45 live but per-status breakdown not enumerated this session
- **`docs/POST_LAUNCH_LINT_DEBT.md`** — Whether the specific file:line lint sites (AdminSidebar.tsx:642/792/804, settings/billing/page.tsx:208-209, PaymobInvoiceModal.tsx:29-30, CenterHealthPanel.tsx:54, settings/referrals/page.tsx:135) still hold at those lines — not re-verified; flagged as possibly shifted
- **`docs/BUILD_COST_FINDINGS.md`** — Current internals of ci.yml/test.yml (playwright on-demand gating, concurrency, vercel.json ignoreCommand) — not re-verified against current workflow files this session
- **`docs/briefs/2026-07-15_skills_pack_and_rules.md`** — Whether PR was actually opened/merged per the brief's hold-for-review flow (cannot inspect git/PR history in this pass; brief was doc-only anyway)
- **`docs/briefs/2026-07-16_job4_money_batch.md`** — Whether every listed test (commission regression tests, C1 toast fix) actually landed in code — not re-audited this pass; out of doc-sync scope
- **`docs/findings/2026-07-15_sweep.md`** — The doc's code-audit conclusions (commission bugs fixed, C1 gating, C2 self-activation) were not re-traced against source this pass; treated as of 2026-07-16
- **`docs/audit/PAYMOB_LAUNCH_READINESS.md`** — Section 1-4 webhook internals (HMAC fail-closed, webhook_inbox unique idempotency index, finalizer paths, line numbers) were not re-verified against live code this pass — labeled as of the 2026-05-23 audit in the banner
- **`docs/audit/SET_PIN_ONBOARDING_DESIGN.md`** — Whether the design's Section-5 per-phone login-lockout shim was implemented — not audited this pass (it was flagged as a separate required fix)
- **`docs/audit/SET_PIN_ONBOARDING_DESIGN.md`** — Whether the fallback link route / pin_setup_tokens table shipped exactly as designed — not re-verified
- **`docs/audit/WHATSAPP_LAUNCH_READINESS.md`** — Section 2-5 env-alias precedence, HMAC fail-closed behavior, and per-file line numbers not re-verified against live code this pass — labeled as of 2026-05-23
- **`docs/CenterHQ_Business_Plan_v5.md`** — The forward revenue projections themselves (center counts, MRR) are modeling assumptions, not live-checkable; left as historical under the banner
- **`docs/EH_GROUP_MASTER_CONTEXT_v24.md`** — Intentional-design decisions 1-12 are narrative product decisions; preserved as record and not individually live-verified beyond the pricing/tenancy items above
- **`docs/EH_GROUP_STRATEGIC_ROADMAP.md`** — 5-year strategic milestones (Mauritius holding co, Georgia LLC, EHG Intelligence commercialization) are future plans, not live-checkable